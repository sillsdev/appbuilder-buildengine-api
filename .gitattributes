# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files that should always have unix line endings
*.sh text eol=lf
application/init text eol=lf
application/yii text eol=lf
build/* text eol=lf
